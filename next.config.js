/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      'images.unsplash.com',
      'plus.unsplash.com',
      'media.graphassets.com',
    ],
  },
};

module.exports = nextConfig;
